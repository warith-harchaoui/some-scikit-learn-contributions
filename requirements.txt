# Runtime requirements for the contribution materials.
# Pin to a recent scikit-learn so the draft estimator (`pr_hddc/_hddc.py`)
# can use the modern `validate_data` API.
numpy>=1.24
scipy>=1.10
scikit-learn>=1.6
matplotlib>=3.8

# Test dependency (the pr_*/test_*.py files run under pytest).
pytest>=7.0
